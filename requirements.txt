# Deepfake Detector — Python 3.13 stack (FastAPI + vanilla JS)
# The model deepfake_finetuned.keras was saved with Keras 3.13.2, which
# requires Python >= 3.11. This venv uses Python 3.13 so the model loads
# natively with no patching.
Python Version==3.13.5
tensorflow==2.21.0
keras==3.13.2

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
python-multipart>=0.0.9

numpy>=1.26,<2.3
pillow>=10.0
